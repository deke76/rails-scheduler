<%= bootstrap_form_with(model: address, class: "needs-validation", novalidate: true, data: { turbo: true, controller: "form-actions" }) do |form| %>
  <div data-controller="address-autocomplete">
    <%= render 'shared/form_errors', record: address %>

    <div class="form-group mb-3">
      <input id="autocomplete" placeholder="Enter your address" type="text" class="form-control" data-address-autocomplete-target="autocomplete" required />
      <div class="invalid-feedback">
        Please enter a valid address.
      </div>
      <%= turbo_frame_tag "address_autocomplete_results" do %>
        <div class="list-group mt-2" data-address-autocomplete-target="results">
        </div>
      <% end %>
    </div>

    <div class="form-group mb-3" data-address-autocomplete-target="mapPreview">
      <% if address.map_image.attached? %>
        <%= image_tag address.map_image, class: "img-fluid rounded" %>
      <% end %>
    </div>

    <%# Hidden fields for address components %>
    <%= form.hidden_field :name, data: { address_autocomplete_target: "name" } %>
    <%= form.hidden_field :unit_number, data: { address_autocomplete_target: "unitNumber" } %>
    <%= form.hidden_field :street_number, id: "street_number", data: { address_autocomplete_target: "streetNumber" } %>
    <%= form.hidden_field :street, id: "street", data: { address_autocomplete_target: "street" } %>
    <%= form.hidden_field :city, id: "city", data: { address_autocomplete_target: "city" } %>
    <%= form.hidden_field :province, id: "province", data: { address_autocomplete_target: "province" } %>
    <%= form.hidden_field :country, id: "country", data: { address_autocomplete_target: "country" } %>
    <%= form.hidden_field :postal_code, id: "postal_code", data: { address_autocomplete_target: "postalCode" } %>
    <%= form.hidden_field :latitude, data: { address_autocomplete_target: "latitude" } %>
    <%= form.hidden_field :longitude, data: { address_autocomplete_target: "longitude" } %>

    <%= render 'shared/form_actions', 
        record: address,
        index_path: addresses_path,
        delete_path: address_path(address) %>
  </div>
<% end %>
