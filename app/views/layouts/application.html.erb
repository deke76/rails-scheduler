<!DOCTYPE html>
<html>
  <head>
    <title>Hockey Scheduler</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= action_cable_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="container-fluid d-flex flex-column justify-contents-between vh-100 px-0">
      <%= render "nav/top_nav" %>
      <main class="mb-auto w-100 overflow-auto">
        <div class="mx-auto container-xl px-md-4 px-lg-5 py-4">
          <%= yield %>
        </div>
      </main>
      <%= render "shared/footer" %>
    </div>
    <div id="flash-container">
      <% flash.each do |key, value| %>
        <%= render "shared/flash", level: key, content: value %>
      <% end %>
    </div>
  </body>
</html>
