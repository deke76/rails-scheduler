<div class="d-flex flex-column justify-content-between h-100">
  <div>
    <h1>Welcome to the Scheduling Website</h1>
    <p>Manage your schedules efficiently and effectively.</p>
  </div>
  <% if user_signed_in? %>
      <%= render "authenticated" %>
  <% else %>
      <%= render "unauthenticated" %>
    <% end %>
  </div>