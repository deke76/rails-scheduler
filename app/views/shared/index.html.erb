<%= turbo_frame_tag "new_#{controller.controller_name.singularize}" %>

<div id="<%= controller.controller_name.pluralize %>">
  <% instance_variable_get("@#{controller.controller_name.pluralize}").each do |item| %>
    <%= render "#{controller.controller_name}/#{controller.controller_name.singularize}", controller.controller_name.singularize.to_sym => item %>
  <% end %>
</div>

<%= link_to send("new_#{controller.controller_name.singularize}_path"), 
    id: "fab",
    class: "btn btn-primary rounded-circle position-fixed d-flex align-items-center justify-content-center fab",
    data: { 
      turbo_frame: "new_#{controller.controller_name.singularize}",
      action: "click->fab#hide",
      controller: "fab"
    } do %>
  +
<% end %> 